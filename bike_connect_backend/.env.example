# Bike Connect Backend - environment variables (example)
# Copy to .env and fill values in your deployment environment.

# REQUIRED: PostgreSQL connection string (SQLAlchemy-compatible).
# Example (local): postgresql+psycopg://appuser:password@localhost:5000/myapp
DATABASE_URL=

# REQUIRED: used to sign JWT access tokens.
JWT_SECRET=

# OPTIONAL: token lifetime in minutes (default: 10080 = 7 days)
JWT_EXPIRES_MINUTES=10080

# OPTIONAL: allowed origins for CORS (comma-separated). If not provided, defaults to "*".
# Example: CORS_ALLOW_ORIGINS=http://localhost:3000,https://your-site.com
CORS_ALLOW_ORIGINS=
